{
  "name": "build-scripts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "tap --ts --reporter=spec --coverage-report=html --coverage-report=text --100 --no-browser test/*.spec.ts",
    "postinstall": "tsc && chmod +x ./bin/index.js"
  },
  "bin": {
    "build-scripts": "./bin/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^11.0.0",
    "octokit": "^3.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/tap": "^15.0.8",
    "tap": "^16.3.8"
  }
}
